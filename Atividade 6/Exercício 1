'''1 - Crie um programa que gere senhas aleatórias com letras, números e símbolos e que o usuário  também escolha o tamanho da senha  para criar senhas seguras automaticamente.'''

# Gerador de Senhas Aleatórias
import random
import string
def gerar_senha(tamanho: int) -> str:
    caracteres = string.ascii_letters + string.digits + string.punctuation # Letras maiúsculas, minúsculas, números e símbolos
    senha = ''.join(random.choice(caracteres) for _ in range(tamanho)) # Gerar senha aleatória
    return senha
try:
    tamanho_senha = int(input("Digite o tamanho da senha desejada: "))
    senha_gerada = gerar_senha(tamanho_senha)
    print(f"Senha gerada: {senha_gerada}")
except ValueError:
    print("Por favor, insira um número válido para o tamanho da senha.")
